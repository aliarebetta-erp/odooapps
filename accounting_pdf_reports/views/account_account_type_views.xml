<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View account.account.type View List -->
    <record id="view_account_account_type_list" model="ir.ui.view">
        <field name="name">view.account.account.type.list</field>
        <field name="model">account.account.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View account.account.type form -->
    <record id="view_account_account_type_form" model="ir.ui.view">
        <field name="name">view.account.account.type.form</field>
        <field name="model">account.account.type</field>
        <field name="arch" type="xml">
            <form string="Account Account Type">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View account.account.type search -->
    <record id="view_account_account_type_search" model="ir.ui.view">
        <field name="name">view.account.account.type.search</field>
        <field name="model">account.account.type</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action account.account.type -->
    <record id="action_account_account_type" model="ir.actions.act_window">
        <field name="name">Account Account Type</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">account.account.type</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Account Account Type.
            </p>
        </field>
    </record>

</odoo>
